import 'package:flutter/material.dart';

class DoctorAppointmentAppBar extends StatelessWidget {
  const DoctorAppointmentAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      leading: const SizedBox(),
    );
  }
}
